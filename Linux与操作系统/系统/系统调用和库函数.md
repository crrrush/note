背景：

系统向下将I/O设备（对应驱动程序）、主存和处理器通过文件、虚拟内存、进程和指令集架构组织管理起来



为了对外表现为一个整体，会隐蔽系统内部的实现，保证封装性。但为了向上提供服务会暴露调用接口，供上层开发使用，这部分有操作系统提供的接口，叫做系统调用

系统调用在使用上功能比较基础，对用户的要求也比较高。所以会有开发者针对特定语言对部分系统调用进行适度封装，从而形成库。这就成了语言提供的库。

