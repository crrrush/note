# 概念及定义

## 概念

**继承**（Inheritance）机制是面对对象程序设计**使代码可以复用**的最重要的手段，它允许程序员在**保持原有类特性的基础上进行扩展**，增加功能，这样产生新的类，称派生类。继承**呈现了面向对象程序设计的层次结构**，体现了由简单到复杂的认知过程。以前我们接触的复用都是函数复用，**继承是类设计层次的复用**。

## 定义

### 格式

### 继承关系和访问限定符

### 继承基类成员访问方式的变化

继承关系表

`private`本质不想被继承

总结：

1. 基类private成员在派生类中无论以什么方式继承都是不可见的。这里的**不可见是指基类的私有成语还是被继承到了派生类对象中，但是语法上限制派生类对象不管在类里面还是类外面都不能去访问它。**
2. 基类private成员在派生类中是不能被访问，如果基类成员不想在类外直接被访问，但需要在派生类中能被访问，就使用protected访问限定符。**可以看出protected访问限定符是因为继承才出现的。**
3. 实际上面的表格我们进行一下总结会发现，基类的私有成员在子类都是不可见。基类的其他成员在子类的访问方式 == Min(成员在基类的访问限定符，继承方式），访问权限：public > protected > private。
4. 使用关键字class时默认的继承方式是private，使用struct时默认的继承方式是public，**不过最好明确写出继承方式**。
5. 在**实际运用中一般使用的都是`public`继承，几乎很少使用`protected`/`private`继承**，也不提倡使用`protected`/`private`继承，因为`protected`/`private`继承下来的成员都只能在派生类的类里面使用，实际扩展和维护性不强。



# 基类和派生类对象赋值转换

以下场景默认公有继承



类型转换具体过程

这里没有哦

直接切片（赋值兼容转换）

使用父类引用或者指针直接访问子类对象父类的部分



父类对象与子类对象的互相赋值问题

子类可以赋值给父类

父类不可赋值给子类（暂时这么说？）





# 作用域

子类和父类有同名成员？

基类和派生类有独立的作用域



同名成员构成隐藏关系



尽量不定义同名成员



# 默认成员函数



派生类对象内有个基类成员

对应地调用基类的默认成员函数

调用方式
初始化列表直接使用类名
域作用限定符

以及
析构函数有点特殊处理
所有类的析构函数编译器角度下名字都是`destructor`
编译器会自行调用

# 继承与友元

**友元不能继承**



# 继承与静态成员

静态成员属于类，不属于类的任何一个成员
或者说静态成员实际存储在静态区，加上域作用限定符（并且是public成员）随时随地可以访问
所以子类继承父类也可以看到



# 多继承/菱形继承

## 多继承

继承多个类


## 菱形继承

继承的两个类有继承了同一个类

造成了数据冗余/二义性

菱形继承的初始化顺序和析构顺序

## 虚继承

解决菱形继承的问题

## 菱形虚拟继承

原来冗余的数据变成了一份，且存储方式发生了变化

# 面试题
1. 什么是菱形继承？菱形继承的问题是什么？  
2. 什么是菱形虚拟继承？如何解决数据冗余和二义性的  
3. 继承和组合的区别？什么时候用继承？什么时候用组合？






