反向迭代器



正向迭代器

每个类都实现？

可以将反向迭代器单独抽离出来

封装一个类的正向迭代器成反向迭代器

代码复用

不能--操作的迭代器，很明显就实现不了反向迭代器了



解引用策略

想想end和begin，end的位置不是有效位置了

拿end构造rbegin呢？是换end -1（类的迭代器万一不支持加减操作？）构造还是解引用时--？

第一个处理方式是否有点不妥，每个类提供函数接口的时候是否别扭

end/begin  rend/rbegin位置是错位还是对称？

肯定对称更好



了解

大佬设计思路